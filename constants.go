package main

//TagError used to tag non-panic error messages
const TagError string = "ERROR"

//TagLog used to tag logging messages
const TagLog string = "FR_RESOURCES"

//TagDiff used to tag generated patches between local and remote documents
const TagDiff string = "DIFF"
